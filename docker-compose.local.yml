services:
  db:
    image: postgres:16-alpine
    environment:
      - 'POSTGRES_USER=user'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_DB=mydb'
    ports:
      - '6432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: